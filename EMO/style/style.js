const authenticitySpan = document.getElementById('authenticitySpan');
const authenticityNav = document.getElementById('authenticityNav');

authenticitySpan.addEventListener('mouseover', () => {
  authenticityNav.style.visibility = 'visible';
})

authenticitySpan.addEventListener('mouseout', () => {
  authenticityNav.style.visibility = 'hidden';
})

const performanceSpan = document.getElementById('performanceSpan');
const glamNav = document.getElementById('glamNav');

performanceSpan.addEventListener('mouseover', () => {
  glamNav.style.visibility = 'visible';
})

performanceSpan.addEventListener('mouseout', () => {
  glamNav.style.visibility = 'hidden';
})



const peteSpan = document.getElementById('peteSpan');
const peteImg = document.getElementById('peteImg');

peteSpan.addEventListener('mouseover', () => {
  peteImg.style.visibility = 'visible';
})

peteSpan.addEventListener('mouseout', () => {
  peteImg.style.visibility = 'hidden';
})



const glamSpan = document.getElementById('glamSpan');

glamSpan.addEventListener('mouseover', () => {
  glamNav.style.visibility = 'visible';
})

glamSpan.addEventListener('mouseout', () => {
  glamNav.style.visibility = 'hidden';
})



const mascRootsSpan = document.getElementById('mascRootsSpan');
const getUpKidsImg = document.getElementById('getUpKidsImg');

mascRootsSpan.addEventListener('mouseover', () => {
  getUpKidsImg.style.visibility = 'visible';
})

mascRootsSpan.addEventListener('mouseout', () => {
  getUpKidsImg.style.visibility = 'hidden';
})



const atTheDriveInSpan = document.getElementById('atTheDriveInSpan');
const catsuitsText = document.getElementById('catsuitsText');

atTheDriveInSpan.addEventListener('mouseover', () => {
  catsuitsText.style.visibility = 'visible';
})

atTheDriveInSpan.addEventListener('mouseout', () => {
  catsuitsText.style.visibility = 'hidden';
})


const sexinessSpan = document.getElementById('sexinessSpan');
const sleepImg = document.getElementById('sleepImg');

sexinessSpan.addEventListener('mouseover', () => {
  sleepImg.style.visibility = 'visible';
})

sexinessSpan.addEventListener('mouseout', () => {
  sleepImg.style.visibility = 'hidden';
})


const thirdWaveSpan = document.getElementById('thirdWaveSpan');
const gerardNav = document.getElementById('gerardNav');

thirdWaveSpan.addEventListener('mouseover', () => {
  gerardNav.style.visibility = 'visible';
})

thirdWaveSpan.addEventListener('mouseout', () => {
  gerardNav.style.visibility = 'hidden';
})


const revivalSpan = document.getElementById('revivalSpan');

revivalSpan.addEventListener('mouseover', () => {
  gerardNav.style.visibility = 'visible';
})

revivalSpan.addEventListener('mouseout', () => {
  gerardNav.style.visibility = 'hidden';
})



const magazineSpan = document.getElementById('magazineSpan');
const magazineImg = document.getElementById('magazineImg');

magazineSpan.addEventListener('mouseover', () => {
  magazineImg.style.visibility = 'visible';
})

magazineSpan.addEventListener('mouseout', () => {
  magazineImg.style.visibility = 'hidden';
})



const journalismSpan = document.getElementById('journalismSpan');
const journalismNav = document.getElementById('journalismNav');

journalismSpan.addEventListener('mouseover', () => {
  journalismNav.style.visibility = 'visible';
})

journalismSpan.addEventListener('mouseout', () => {
  journalismNav.style.visibility = 'hidden';
})



const trinitySpan = document.getElementById('trinitySpan');
const trinityNav = document.getElementById('trinityNav');

trinitySpan.addEventListener('mouseover', () => {
  trinityNav.style.visibility = 'visible';
})

trinitySpan.addEventListener('mouseout', () => {
  trinityNav.style.visibility = 'hidden';
})



const powerfulSpan = document.getElementById('powerfulSpan');
const desireNav = document.getElementById('desireNav');

powerfulSpan.addEventListener('mouseover', () => {
  desireNav.style.visibility = 'visible';
})

powerfulSpan.addEventListener('mouseout', () => {
  desireNav.style.visibility = 'hidden';
})