const titleSpan = document.getElementById('titleSpan');
const titlePopup = document.getElementById('titlePopup');

titleSpan.addEventListener('mouseover', () => {
  titlePopup.style.visibility = 'visible';
})

titleSpan.addEventListener('mouseout', () => {
  titlePopup.style.visibility = 'hidden';
})


const godSpan = document.getElementById('godSpan');
const godPopup = document.getElementById('godPopup');

godSpan.addEventListener('mouseover', () => {
  godPopup.style.visibility = 'visible';
})

godSpan.addEventListener('mouseout', () => {
  godPopup.style.visibility = 'hidden';
})


const wrongsSpan = document.getElementById('wrongsSpan');
const wrongsPopup = document.getElementById('wrongsPopup');

wrongsSpan.addEventListener('mouseover', () => {
  wrongsPopup.style.visibility = 'visible';
})

wrongsSpan.addEventListener('mouseout', () => {
  wrongsPopup.style.visibility = 'hidden';
})


const whoDoesSpan = document.getElementById('whoDoesSpan');
const whoDoesPopup = document.getElementById('whoDoesPopup');

whoDoesSpan.addEventListener('mouseover', () => {
  whoDoesPopup.style.visibility = 'visible';
})

whoDoesSpan.addEventListener('mouseout', () => {
  whoDoesPopup.style.visibility = 'hidden';
})


const keysSpan = document.getElementById('keysSpan');
const keysPopup = document.getElementById('keysPopup');

keysSpan.addEventListener('mouseover', () => {
  keysPopup.style.visibility = 'visible';
})

keysSpan.addEventListener('mouseout', () => {
  keysPopup.style.visibility = 'hidden';
})


const thanksSpan = document.getElementById('thanksSpan');
const thanksPopup = document.getElementById('thanksPopup');

thanksSpan.addEventListener('mouseover', () => {
  thanksPopup.style.visibility = 'visible';
})

thanksSpan.addEventListener('mouseout', () => {
  thanksPopup.style.visibility = 'hidden';
})


const sweeterSpan = document.getElementById('sweeterSpan');
const sweeterPopup = document.getElementById('sweeterPopup');

sweeterSpan.addEventListener('mouseover', () => {
  sweeterPopup.style.visibility = 'visible';
})

sweeterSpan.addEventListener('mouseout', () => {
  sweeterPopup.style.visibility = 'hidden';
})


const standOffSpan = document.getElementById('standOffSpan');
const standOffPopup = document.getElementById('standOffPopup');

standOffSpan.addEventListener('mouseover', () => {
  standOffPopup.style.visibility = 'visible';
})

standOffSpan.addEventListener('mouseout', () => {
  standOffPopup.style.visibility = 'hidden';
})


const numbersSpan = document.getElementById('numbersSpan');
const numbersPopup = document.getElementById('numbersPopup');

numbersSpan.addEventListener('mouseover', () => {
  numbersPopup.style.visibility = 'visible';
})

numbersSpan.addEventListener('mouseout', () => {
  numbersPopup.style.visibility = 'hidden';
})


const pageSixSpan = document.getElementById('pageSixSpan');
const pageSixPopup = document.getElementById('pageSixPopup');

pageSixSpan.addEventListener('mouseover', () => {
  pageSixPopup.style.visibility = 'visible';
})

pageSixSpan.addEventListener('mouseout', () => {
  pageSixPopup.style.visibility = 'hidden';
})


const linerSpan = document.getElementById('linerSpan');
const linerPopup = document.getElementById('linerPopup');

linerSpan.addEventListener('mouseover', () => {
  linerPopup.style.visibility = 'visible';
})

linerSpan.addEventListener('mouseout', () => {
  linerPopup.style.visibility = 'hidden';
})


const moodSpan = document.getElementById('moodSpan');

moodSpan.addEventListener('mouseover', () => {
  linerPopup.style.visibility = 'visible';
})

moodSpan.addEventListener('mouseout', () => {
  linerPopup.style.visibility = 'hidden';
})